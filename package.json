{
  "name": "vibedesign-extension",
  "private": true,
  "version": "2.0.1",
  "description": "Tooling for the VibeDesign Chrome extension. The extension itself has no build step and ships no dependencies — this file exists only for the capture harness and test scripts.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "capture": "node scripts/capture.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
